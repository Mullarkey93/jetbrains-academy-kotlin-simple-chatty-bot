import java.util.*

fun main(args: Array<String>) {
    val scanner = Scanner(System.`in`)
    // write your code here
    val character_one = scanner.next()
    val character_two = scanner.next()
    println(character_one.toUpperCase() == character_two.toUpperCase())

}